let course = ["hld", "lld", "dsa"];
console.log(course);
console.log(typeof course);
// type is object
// arrays are object, because array and functions are object

console.log(course[0]);
console.log(course[1]);
console.log(course[2]);
/* 
Unique this in JS is, for array both type and length
can be dynamic, but if we see other languages 
like c, c++ or java, the type of the values should
have to be same, but in JS it does not matter.
*/

console.log(course[3]);
// undefined or null value because there is nothing at that index.
